{
  "name": "inspired-products",
  "version": "0.1.0",
  "private": true,
  "description": "Hands-on projects for learning data science, ad tech, and product thinking.",
  "scripts": {
    "test": "npm --prefix apps/campaign-lab test && npm --prefix agents/linkedin-post-coach test",
    "check": "npm --prefix apps/campaign-lab run check && npm --prefix agents/linkedin-post-coach run check",
    "test:product-sense": "cd agents/product-sense-mock && python -m unittest discover -s tests -t .",
    "test:decision-log": "cd agents/decision-log && python -m unittest discover -s tests -t ."
  }
}
